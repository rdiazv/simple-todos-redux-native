import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  Todo: {
    borderColor: '#ccc',
    borderStyle: 'solid',
    borderWidth: 1,
    marginTop: -1,
    padding: 10
  }
});
